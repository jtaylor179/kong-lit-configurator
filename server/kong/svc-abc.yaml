_format_version: "1.1"
_info:
  defaults: {}
  select_tags:
  - svc-abc
services:
- connect_timeout: 60000
  enabled: true
  host: httpbin.org
  name: abc
  path: /anything
  plugins:
  - config:
      credentials: false
      exposed_headers: null
      headers: null
      max_age: null
      methods:
      - DELETE
      origins: null
      preflight_continue: false
    enabled: true
    name: cors
    protocols:
    - grpc
    - grpcs
    - http
    - https
  port: 80
  protocol: https
  read_timeout: 60000
  retries: 5
  tags:
  - svc-abc
  write_timeout: 60000
